apiVersion: v1
kind: Service
metadata:
  labels:
    app: reservations
  name: reservations
{{- with .Values.services.reservations.annotations }}
  annotations:
{{ toYaml . | nindent 4 }}
{{- end }}
spec:
  ports:
    - name: 'http'
      port: 3004
      protocol: TCP
      targetPort: 3004
  selector:
    app: reservations
  type: NodePort
